[project]
name = "embedding-analysis"
version = "0.1.0"
description = ""
authors = [{name = "Warren Davies"}]
readme = "README.md"
requires-python = ">=3.12"

[tool.poetry]
packages = [{include = "embeddinganalysis", from = "src"}]

[tool.poetry.dependencies]
torch = ">=2.13.0,<3.0.0"
transformers = "^5.15.0"
accelerate = "^1.14.0"
numpy = "2.4"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
